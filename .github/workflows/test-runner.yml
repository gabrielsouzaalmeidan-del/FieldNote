name: Test Runner

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/test-runner.yml'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Runner OK
        run: echo "Runner respondendo. Repo=${{ github.repository }} Ref=${{ github.ref }}"
